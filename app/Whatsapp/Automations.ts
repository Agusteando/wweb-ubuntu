export default class Automations {
  public static async run() {
    // Automations are now completely abstracted as configurable Modules. 
    // You can manage them per-client in the UI just like Commands.
  }
}